[package]
name = "n-body-problem-simulation"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4.14"
micro-ui = { path = "../../micro-ui" }

# Always optimize dependencies (recommended for macroquad)
[profile.dev.package.'*']
opt-level = 3
